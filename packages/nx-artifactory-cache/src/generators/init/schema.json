{
  "$schema": "http://json-schema.org/schema",
  "$id": "Init",
  "title": "Add Artifactory Cache configuration to NX workspace",
  "type": "object",
  "properties": {
    "url": {
      "type": "string",
      "description": "Artifactory base URL",
      "x-prompt": "Artifactory base URL"
    },
    "basicHttpAuth": {
      "type": "string",
      "description": "API Key Auth credentials for Artifactory",
      "x-prompt": "API Key Auth credentials for Artifactory"
    },
    "repoKey": {
      "type": "string",
      "description": "Artifactory repository key",
      "x-prompt": "Artifactory repository key"
    }
  }
}
